import styled from '@emotion/styled';

export const ContentContainer = styled.div`
  height: 100%;
  display: flex;
  gap: 1rem;
  padding: 1rem;
  background-color: ${({ theme }) => theme.palette.cnps.backgroundColor};
`;
